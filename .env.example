# Somente a API PostgreSQL e o teste de integração precisam desta variável.
# A execução local com SQLite (`python3 start.py`) não precisa de credenciais.
# Nunca coloque uma senha real neste arquivo versionado.
DATABASE_URL=postgresql://USUARIO:SENHA@HOST-POOLER/BANCO?sslmode=require
